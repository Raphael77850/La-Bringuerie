# ===============================================
# CONFIGURATION SÉCURISÉE - La Bringuerie v2.0
# ===============================================

# APPLICATION
NODE_ENV=development
PORT=3001
APP_VERSION=2.0.0

# ⚠️ SÉCURITÉ JWT - GÉNÉRER DES SECRETS FORTS EN PRODUCTION
# Utilisez: openssl rand -base64 32
JWT_SECRET=your_super_secure_jwt_secret_minimum_32_characters_long_change_in_production
JWT_REFRESH_SECRET=your_super_secure_refresh_secret_minimum_32_characters_long_change_in_production

# BASE DE DONNÉES
# Railway/Production
MYSQLHOST=your_mysql_host
MYSQLPORT=3306
MYSQLUSER=your_mysql_user
MYSQLPASSWORD=your_mysql_password
MYSQLDATABASE=your_mysql_database

# Développement local
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_local_password
DB_NAME=la_bringuerie_dev

# SÉCURITÉ CORS (Production - domaines autorisés)
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com,https://your-staging.com

# LOGGING
LOG_LEVEL=info
LOG_TO_FILE=true

# RATE LIMITING
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_AUTH_MAX=5

# UPLOAD (si nécessaire)
UPLOAD_MAX_SIZE=10485760
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/webp
UPLOAD_PATH=./public/uploads/events

# EMAIL (si nécessaire)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# MONITORING (si nécessaire)
SENTRY_DSN=
DATADOG_API_KEY=

# ===============================================
# INSTRUCTIONS DE SÉCURITÉ CRITIQUES
# ===============================================
# 1. ⚠️ JAMAIS commiter ce fichier avec de vraies valeurs
# 2. ⚠️ Générer des secrets JWT de 32+ caractères en production
# 3. ⚠️ Utiliser HTTPS en production uniquement
# 4. ⚠️ Configurer un reverse proxy (nginx/cloudflare)
# 5. ⚠️ Activer le monitoring en production
# 6. ⚠️ Effectuer des sauvegardes régulières de la DB
# 7. ⚠️ Mettre à jour les dépendances régulièrement
# ===============================================
